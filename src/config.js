const config = {
  baseURL: "http://localhost:3333", // Substitua pela URL do seu servidor
  imagePathTenants: "/uploads/tenants", // Subcaminho onde as imagens estão armazenadas
};

export default config;
